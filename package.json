{
  "name": "metropolis-root",
  "version": "1.0.0",
  "description": "Root package for Metropolis project Automation",
  "scripts": {
    "dev": "docker compose up -d && docker compose watch",
    "build": "docker compose build",
    "up": "docker compose up",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "prod:dev": "docker compose -f docker-compose.prod.yml down && docker compose -f docker-compose.prod.yml build && docker compose -f docker-compose.prod.yml up -d && docker compose -f docker-compose.prod.yml logs -f",
    "clean": "docker compose down --rmi all --volumes"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
