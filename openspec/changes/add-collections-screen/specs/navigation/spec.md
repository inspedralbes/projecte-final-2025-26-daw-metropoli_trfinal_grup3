## ADDED Requirements

### Requirement: Navigation to Collections
The main navigation bar SHALL provide a link/button to the `/colections` screen, replacing the previous direct link to the user profile.

#### Scenario: User clicks the collections icon in the navbar
- **WHEN** user clicks the designated collections icon in the main navigation bar
- **THEN** the system navigates the user to the `/colections` screen
