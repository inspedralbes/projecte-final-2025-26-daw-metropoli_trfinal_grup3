## 1. Global Header Component

- [x] 1.1 Create `Header.jsx` in `front/src/layouts`
- [x] 1.2 Implement the avatar retrieval logic (`localStorage` and `getAvatarUrl`)
- [x] 1.3 Add dynamic title rendering based on `useLocation().pathname` (WeMap, Comunitat, Rutas)
- [x] 1.4 Ensure the title uses `font-display` and minimal top-left margin
- [x] 1.5 Add specific black text styling for the `/` (Map) route
- [x] 1.6 Implement the profile `Link` wrapping the user avatar on the top right

## 2. Integration into Pages

- [x] 2.1 Replace custom header in `Home.jsx` with the new `<Header />` component
- [x] 2.2 Replace custom header in `Map.jsx` with the new `<Header />` component
- [x] 2.3 Replace custom header in `Community.jsx` with the new `<Header />` component
- [x] 2.4 Replace custom header in `Collections.jsx` with the new `<Header />` component
