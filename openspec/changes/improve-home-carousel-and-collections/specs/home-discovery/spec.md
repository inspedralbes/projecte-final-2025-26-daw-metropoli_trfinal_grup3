## MODIFIED Requirements

### Requirement: Interactive home discovery
The home page carousel MUST be interactive. Each route showcased SHALL act as a gateway to the Map experience.

#### Scenario: Visual feedback on hover
- **WHEN** the user hovers over a carousel image
- **THEN** the image MUST show a subtle zoom or brightness effect
- **AND** the cursor MUST change to a pointer
